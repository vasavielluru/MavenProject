pipeline {
    agent any
    tools {
        maven 'Maven'
        
    }
    stages {
       
        

        stage ('Build') {
            steps {
                echo "Building" 
            }
            post {
                success {
                    junit 'target/surefire-reports/**/*.xml' 
                }
            }
        }
    }
}
